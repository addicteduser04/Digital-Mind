import Link from "next/link"; import { AppShell } from "@/components/app-shell"; import { HabitForm } from "@/components/behavior-forms"; import { getCurrentUserId } from "@/server/auth/current-user"; import { getHabitOptions } from "@/server/repositories/behavior";
export const dynamic = "force-dynamic"; export default async function NewHabitPage() { const areas = await getHabitOptions(await getCurrentUserId()); return <AppShell><div className="mx-auto max-w-3xl"><Link href="/habits" className="text-xs text-muted-foreground">Habits /</Link><h1 className="my-7 text-3xl font-medium">New habit</h1><HabitForm lifeAreas={areas.map((x) => ({ id: x.id, label: x.name }))} /></div></AppShell>; }
