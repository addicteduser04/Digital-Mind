import Link from "next/link"; import { notFound } from "next/navigation"; import { AppShell } from "@/components/app-shell"; import { ScheduleTaskForm } from "@/components/calendar-forms"; import { getCurrentUserId } from "@/server/auth/current-user"; import { getOwnedTask } from "@/server/repositories/execution";
export const dynamic = "force-dynamic"; export default async function SchedulePage({ params, searchParams }: { params: Promise<{ id: string }>; searchParams: Promise<{ date?: string }> }) { const [{ id }, { date }] = await Promise.all([params, searchParams]); const task = await getOwnedTask(await getCurrentUserId(), id); if (!task) notFound(); return <AppShell><div className="mx-auto max-w-3xl"><Link href="/calendar" className="text-xs text-muted-foreground">Calendar /</Link><h1 className="my-7 text-3xl font-medium">Schedule task</h1><ScheduleTaskForm task={task} initialDate={date} /></div></AppShell>; }
